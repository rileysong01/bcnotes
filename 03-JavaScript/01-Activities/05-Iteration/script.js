var classmates;

var classmates = ["Darw", "Darwin", "Darweenie", "Melly", "Melon"];

//for-loops execute a block of code more than once
    //loop continues as long is index < length of classmates variable
    //index increments by 1 after every iteratoin
for (var index = 0; index < classmates.length; index++) {
    console.log("Great to see you, " + classmates[index] + "!")
    
}

